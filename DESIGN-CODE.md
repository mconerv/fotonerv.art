# FotoNerv — дизайн-код личного бренда

Макс Карпович · фотограф и контент-креатор
Санкт-Петербург · съёмки по всей России · `fotonerv.art`

Палитра и акцент выведены из основного логотипа (знак с визиток).

---

## 1. Концепция

Имя = `Foto` + `Нерв`. «Нерв» — сигнал, чувствительность, живое напряжение момента,
«поймать нерв». Позиционирование: кадр с характером и эмоциональным зарядом,
а не стерильный «цифровой скан».

**Сигнатура — живая сигнальная линия (нерв-волна):** тонкая реактивная линия-импульс
проходит сквозь интерфейс — подчёркивает логотип, работает индикатором прокрутки,
пульсирует на ховере карточек и разделяет блоки. Решает разом юзабилити
(ориентирование) и «дружелюбность» (отзывчивость).

---

## 2. Логотип

Основной знак — **круглый портрет-аватар**: фотограф с камерой у лица, в тёмном
кольце на кремовой бумаге. Используется на визитках, поэтому задаёт палитру всего бренда.

Цвета, считанные со знака:

| Элемент | HEX |
|---|---|
| Чёрное кольцо | `#141414` |
| Бумага (фон диска) | `#E9E3D6` |
| Загар / кожа | `#D8C090` |
| Бордовый плед | `#7E2B30` |
| Красный (ободок объектива) | `#B83A33` |

Знак с визиток — **настоящий вектор** (экспорт из CorelDRAW → PDF → SVG, 67 контуров),
масштабируется без потерь. Используется в **двух вариантах**:

| Вариант | Что | Где |
|---|---|---|
| **Full** (с росчерком) | `logo-full.svg` — полный знак, включая хвост-росчерк за кольцом | Шапка, подвал, портрет в «Обо мне» |
| **Badge** (круг) | `logo-badge.svg` — тот же вектор с круговым клипом (без росчерка) | Фавикон, apple-touch, мелкие круглые слоты |

Правила:
- фон знака прозрачный; на графите ложится без ореола;
- в шапке/подвале — связка **знак (full) + леттеринг `FotoNerv` + нерв-подчёркивание**;
- на ховере — мягкое свечение акцентом (`drop-shadow`), форму росчерка не обрезаем;
- крупно — в «Обо мне» как личный портрет с мягкой тенью;
- для фавикона берём **badge** (росчерк на 16–48px нечитаем и мусорит).

Файлы: `assets/logo-full.svg`, `assets/logo-badge.svg` (вектор),
`assets/logo-full.png` 1024², `assets/logo-badge.png` 512² (растровые фолбэки),
`assets/favicon-48.png`, `assets/favicon-32.png`.

> Источник — векторный PDF из CorelDRAW, поэтому контуры чистые (не трассировка).
> Badge получается из того же вектора круговым клипом — обе версии всегда синхронны.

---

## 3. Палитра интерфейса

Тёмный галерейный фон под фотографии; цвет текста = бумага логотипа;
акцент = красный логотипа, поднятый по яркости для контраста на тёмном.

| Токен | HEX | Роль |
|---|---|---|
| `--graphite` | `#15171C` | Основной фон |
| `--carbon` | `#1D2027` | Поверхности, карточки |
| `--carbon-2` | `#23272F` | Вторичная заливка плейсхолдеров |
| `--line` | `#2C313B` | Хэйрлайны, границы, поля форм |
| `--fog` | `#EAE4D6` | Текст (= бумага логотипа) |
| `--ash` | `#8C909B` | Второстепенный текст |
| `--impulse` | `#E23D30` | Акцент «нерв» (красный логотипа, ярче) |
| `--impulse-deep` | `#B2302A` | Ховеры, пик градиента |

Акцент — один. Чёрное кольцо логотипа рифмуется с `--graphite`, кремовая бумага — с `--fog`,
так печатный знак и сайт читаются как одна система.

**Фото под бренд.** Портреты и контент тонируются дуотоном по той же палитре:
тени → графит `#1C1F26`, средние → тёплый `#7A6D5D`, света → крем `#EFE9DB`,
плюс лёгкая виньетка. Так снимки и интерфейс звучат как одно целое
(пример — портрет в «Обо мне», `assets/portrait.jpg`).

---

## 4. Типографика

| Роль | Гарнитура | Вес | Применение |
|---|---|---|---|
| Display | **Unbounded** | 500–700 | Логотип-леттеринг, H1/H2, номера шагов |
| Text | **Manrope** | 400–700 | Основной текст, навигация, кнопки |
| Mono | **JetBrains Mono** | 400–500 | EXIF-подписи (ISO · ƒ · мм), лейблы, «сигнал-ридаут» |

```html
<link href="https://fonts.googleapis.com/css2?family=Unbounded:wght@400;500;600;700&family=Manrope:wght@400;500;600;700&family=JetBrains+Mono:wght@400;500&display=swap" rel="stylesheet">
```

Шкала: H1 `clamp(2.7rem,7vw,5.4rem)` / LH .98; H2 `clamp(1.9rem,4vw,3rem)`;
body 1rem / LH 1.6; eyebrow/caption .72–.78rem, `letter-spacing .1–.18em`, uppercase, mono.

---

## 5. UI-правила

- Радиусы: 12px карточки; 999px чипы и кнопки; 10px поля форм.
- Кнопки: solid (импульс, текст графитовый) — главное действие; outline — вторичное.
- Тап-зоны ≥ 44px; видимый фокус `outline 2px --impulse`.
- `@media (prefers-reduced-motion: reduce)` отключает анимации и reveal; мягкие easing.
- Сетка работ: 3 → 2 → 1 колонки; широкие плитки `span 2` в режиме «Все».
- Плейсхолдеры работ: дуотон-градиент + SVG-зерно — слот под реальные WebP с EXIF-подписью.

---

## 6. Голос бренда

Уверенный, прямой, «на нерве». Без продающей воды и канцелярита; глаголы активные,
регистр sentence case. Опоры: «Ловлю нерв момента», «Кадр с характером — не цифровой скан».

---

## 7. Структура страницы

1. Шапка — аватар + леттеринг + нерв-индикатор прокрутки.
2. Hero — тезис + сигнал-карта с живой нерв-волной, гео-метки, два CTA.
3. Работы — живая превью-стена («Все»: маленькие квадраты). Каждая плитка меняется
   независимо со случайной задержкой 5–10 с; кадры берутся из «колоды» (перемешанный
   полный пул), поэтому со временем в стене показываются все снимки жанров без пропусков.
   `prefers-reduced-motion` отключает ротацию. Выбор жанра заменяет стену полноразмерной
   галереей с лайтбоксом (клик по кадру, стрелки ‹ ›, ESC). Жанры по твоей разбивке:
   Портрет, Репортаж, Архитектура и интерьеры, Пейзаж.
4. Обо мне — тонированный портрет (фото) + текст, счётчики строкой.
5. Как работаем — реальная последовательность из 4 шагов (нумерация оправдана).
6. Соцсети — карточки-переходы: Behance, Instagram, Telegram, Boosty.
7. Контакт — форма заявки + прямые каналы.
8. Подвал — аватар, копирайт.

---

## 8. Интеграция реальных фото

```html
<div class="tile" data-cat="portrait">
  <picture>
    <source srcset="img/portrait-01.webp" type="image/webp">
    <img src="img/portrait-01.jpg" alt="Портрет — описание кадра" loading="lazy">
  </picture>
  <div class="label">Портрет</div>
  <div class="meta"><span>85mm · ƒ1.4 · ISO200</span><!-- pulse svg --></div>
</div>
```
`<img>` заполняет плитку (`position:absolute; inset:0; object-fit:cover`).
Формат `picture + WebP + lazy` и осмысленные alt — как в текущем аудите сайта.

Изображения — внешними файлами (логотип/портрет/фавикон вшиты в `index.html`):
- `img/wall/<name>.webp` (~440px) — отдельный пул для рандомной стены «Все»;
- `img/thumb/<name>.webp` (~600px) — плитки галереи жанров;
- `img/<name>.webp` (полный размер) — открывается в лайтбоксе.
Жанры берутся из папок-категорий (Портрет/Репортаж/Архитектура и интерьеры/Пейзаж).
Счётчик и дублирующее название жанра над галереей убраны. Имена приведены к безопасным.
Для отдачи через файлы — замени `background:url(data:…)` на `url(assets/logo-…png)`
и `href` в `<link rel=icon>` на путь к фавикону.
